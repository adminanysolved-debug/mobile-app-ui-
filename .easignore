# Ignore non-essential root directories to keep build context clean
server/
admin-system/
migrations/
.vscode/
.git/

# Standard ignores
**/node_modules/
**/android/
**/ios/
**/.expo/
**/dist/
**/build/
**/*.apk
**/*.aab
**/*.log

# Allow everything else (root config files, client directory, assets directory)
